架构大改
解耦合执行模块
加入保留站用于处理数据冒险
取指-译码-发射-执行-ROB
访存，乘法，除法，alu，分支处理全都是执行的子模块


注意处理WAW冲突
注意会有一个执行单元同时准备好多条指令的情况
注意每次只允许写和广播一条指令的结果，要做结果的缓冲器
注意0号寄存器没有标志位

分支错误的指令需要广播，但是需要更新寄存器状态和将数据写回的寄存器的值直接广播回去给保留站


IF：
监听分支地址总线
计算下一周期指令的地址

ID：
input:
inst
保留站的busy位
寄存器堆状态位(不用)
输出：
指令发射的保留站目标tag
具体操作op
分支跳转标志位
寄存器地址（没有rd的地址直接让rd=0）
ROB的配置


保留站rstation
alu4行，lsu4行，JBCU1行
input:
寄存器堆状态位
读取的寄存器数据
依赖的tag
监听总线


如果指令不能从逻辑寄存器堆读取到所需数据，
就会根据寄存器结果状态表的查询结果查询数据，
此时会有三个情况：一是数据由正在执行的指令得出，
此时指令需要在保留站中监听CDB；二是数据正在ROB中，
但是没有提交，此时指令会从ROB中读出数据；三是数据正在CDB总线上广播，
此时指令读取CDB的数据；
顺序单发
寄存器状态表
WAW指令也要阻塞


分支必须在一号位发射
分支在2号位就只发射一条
广播和写回需要仲裁

分支输出广播到分支总线
跳转输出广播到分支总线的同时写wb_buffer


stall：
1. 源寄存器未释放
2. lsu_busy

SAT和目的寄存器的0号位不允许修改
15号执行单元设定为无效

分支N重定向使能时，下一周期指令N+1完成计算，所以应该修改ID-EX的发射单元到无效单元,同时禁止写RAT
下一周期N+2译码完成，要修改IF-ID，即无效位，直接传无效指令给ID
下一周期N+3取值完成，直接修改取指的地址即可完成

对于stall
依赖不是ID阶段检查，而是EX执行前检查。
是ID阶段检测到需要stall，这时候应该stall的是IF和ID的寄存器以及指令地址

issue模块
接收ID信号，发送给EX，和EX同周期
监听RAT，CDB，BCB，产生stall信号，以及将错误路径信号发射到垃圾站
监听CDB进行旁路


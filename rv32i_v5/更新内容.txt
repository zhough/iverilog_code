1. 对dram模拟块改为BRAM的控制逻辑，包括dram.v，we作为写的掩码，对齐vivadoip核
2. 修改顶层的控制逻辑，load的地址要在EX阶段传入，数据在MEM阶段读出
3. store阶段
4. load 和 store子模块，包含生成掩码和读出数据的控制
5. 为保持ram的地址统一输入，store要提前到EX阶段，所以需要为写入数据做数据旁路
6. 考虑store after load情况 

7. 如果统一在EX阶段传入store和load,就不需要考虑SAL，但是要考虑WAS
   如果统一在MEM阶段再传入，就要考虑将EX的结果打一拍，并且load-after-use逻辑就会发生改变
   这里使用前一种方案，否则得nop两个周期

8. 重点：peirp_bridge必须改，必须将输出的控制

待定：
irom暂时用lutram
===============================上述已完成==========================================


4.15  23:45改
IF阶段需要将nop的优先级放分支跳转后
